---
title: "还有多久 timeFromNow"
description: "timeFromNow 使用"
---

import CodeRunner from "../../../../components/CodeRunner.astro"

<p>还有多久</p>
<br/>


<head>
  <script src="https://cdn.jsdelivr.net/npm/@zzcpt/zztool@latest/dist/zztool.umd.min.js" defer></script>
</head>

<CodeRunner code={`
const time = zztool.timeFromNow(2758013775599);
console.log(time)
const time1 = zztool.timeFromNow(2758013775599,{
 open:true,
 cb:({minutes}) => {
   const str = minutes + '分钟后'
   return str
 }
});
console.log(time1)
`} />

### props
| 属性名 | 说明 | 类型 | 必填 | 默认值 |
| --- | --- | --- | --- | --- |
| *timestamp* | *时间戳* | *Number* | *✅* | *-* |
| *option* | *配置项* | *Object* | *❌* | *-* |
| *option.open* | *自定义* | *Boolean* | *✅* | *-* |
| *option.cb* | *回调* | *Function* | *✅* | *-* |
{/* | *format* | *格式* | *String* | *❌* | *hh:mm* | */}
```javascript
interface TimeUnits {
  seconds: number;
  minutes: number;
  hours: number;
  days: number;
  months: number;
  years: number;
}
interface Options {
  open:boolean,
  cb:(e:TimeUnits) => string
}
```


### 返回值
返回值