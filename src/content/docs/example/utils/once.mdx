---
title: "一次 once"
description: "once 使用"
---

import CodeRunner from "../../../../components/CodeRunner.astro"

<p>函数只执行一次</p>
<br/>

<head>
  <script src="https://cdn.jsdelivr.net/npm/@zzcpt/zztool" defer></script>
</head>

<CodeRunner code={`
const fn = (num) => {console.log('once:',num)};
const times = zztool.once(fn);
for (let i = 0; i < 10; i++) {
    times(i);
    console.log('i:',i)
}
`} />

### props
| 属性名 | 说明 | 类型 | 必填 | 默认值 |
| --- | --- | --- | --- | --- |
| *Fn* | *回调函数* | *Function* | *✅* | *-* |
{/* | *keys* | *要删除的下标* | *String[]* | *✅* | *-* | */}
{/* | *first* | *首字符* | *String* | *❌* | *空字符* | */}
{/* | *format* | *格式* | *String* | *❌* | *hh:mm* | */}


### 返回值
返回值